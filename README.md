# archlinux-self-config
shell script that automatically configures an arch linux install

### Usage
First, run generate.sh on an existing computer. This will create a self-extract.sh. Move self-extract.sh to a newly installed arch machine and run it. Reboot.


Note: install plasma-meta and konsole DURING arch installation.
